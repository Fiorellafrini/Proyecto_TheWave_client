import React from "react";
import styles from '../SectionCarrito/SectionCarrito.module.css'

const SectionCarrito = () => {

    return(
        <>
        <h1 className={styles.titulo}>SectionCarrito</h1>
        </>
    )
};

export default SectionCarrito;